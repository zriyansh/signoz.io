In a direcrory of your choosing, clone the SigNoz repository and `cd` into the `signoz/deploy` directory by entering the following commands:
  ```bash
git clone https://github.com/SigNoz/signoz.git && cd signoz/deploy/
  ```